apiVersion: backstage.io/v1alpha1
kind: Template
metadata:
  name: wize-demo-data-science
  title: Demo data science template
  description: Create a data science project powered with python
  tags:
    - demo
    - data
    - python
spec:
  owner: web@example.com
  templater: cookiecutter
  type: service
  path: '.'
  schema:
    required:
      - component_id
      - repo_name
      - description
    properties:
      component_id:
        title: Name
        type: string
        description: Unique name of the component
      repo_name:
        title: Repository
        type: string
        description: Repo name of the component
      description:
        title: Description
        type: string
        description: Description of the component
      
